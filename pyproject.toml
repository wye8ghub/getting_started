[project]
name = "getting-started"
version = "0.1.0"
description = "My first python project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cowsay>=6.1",
    "torch>=2.8.0",
    "transformers>=4.56.2",
]
